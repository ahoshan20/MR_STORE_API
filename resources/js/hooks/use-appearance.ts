export function initializeTheme(): void {
    // Placeholder: real implementation can sync theme from server or localStorage
    try {
        // noop for now
    } catch (e) {
        // ignore
    }
}
